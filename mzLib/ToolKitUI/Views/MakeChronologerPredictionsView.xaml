<UserControl x:Class="ToolKitUI.Views.MakeChronologerPredictionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToolKitUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid IsSharedSizeScope="True">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="0">
            <StackPanel Grid.Column="0" Grid.Row="0" Orientation="Vertical"  Margin="5">
                <TextBlock Text="Chronologer HI" FontSize="24" Margin="10"/>
                <Button Click="LoadFileClick" Content="Load File" FontSize="24" Margin="10"/>
                <Button Content="Peek at file" FontSize="24" Margin="10"/>
                <Button Content="Write File" FontSize="24" Margin="10"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <DataGrid Grid.Column="0" Grid.Row="0" Height="auto" Width="auto" Margin="10"/>
        </Grid>

    </Grid>
</UserControl>
